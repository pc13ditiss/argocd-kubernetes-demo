apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-web-two
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-web-two
  template:
    metadata:
      labels:
        app: demo-web-two
    spec:
      containers:
        - name: demo-web-two
          image: nginx:1.25
          ports:
            - containerPort: 80
